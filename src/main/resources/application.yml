server:
  port: 8080

logging:
  level:
    org:
      springframework: debug
    com: 
      zyl: debug

#mybatis:
#  mapper-locations: classpath*:mybatis/*Mapper.xml
#  config-location: classpath*:mybatis-config.xml

#动态数据源初始化，默认关闭，需要显式打开
dynamic-data-source:
  shutdow-dynamic-init: false

#全局读写分离配置，默认false不开启，开启后如果配置了slave，所有select查询都会走slave，
#dynamic-data-source:
#  readonly-slave: true



spring:
  main:
    allow-bean-definition-overriding: true
jdbc:
  url: jdbc:mysql://127.0.0.1:3306/multi_default?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8&useSSL=false








